/**
 * Single source of truth for database types.
 *
 * These are regenerated automatically whenever the schema changes, so the
 * Studio re-exports them rather than keeping a second hand-maintained copy
 * that silently drifts out of date after a migration.
 */
export type {
  Json,
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
} from "@/integrations/supabase/types";
